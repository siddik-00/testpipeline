[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "api-call"
version = "0.1.0"
description = "Simple API call helpers"
requires-python = ">=3.8"
dependencies = ["requests"]

[tool.setuptools]
py-modules = ["api_call"]